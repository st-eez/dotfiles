# True color support
set -g default-terminal "tmux-256color"
set-option -sa terminal-overrides ",xterm*:Tc"

# Start windows and panes at 1, renumber on close
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Prefix: Ctrl+Space (keep Ctrl+b as secondary)
set -g prefix C-Space
set -g prefix2 C-b
bind C-Space send-prefix

# Scrollback buffer (default 2000 is too small for Claude Code sessions)
set -g history-limit 50000

# No escape delay (faster neovim mode switching)
set -sg escape-time 0

# New windows/splits open in current directory
bind c new-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Reload config with prefix + r
bind r source-file ~/.config/tmux/tmux.conf

# Let programs detect pane focus changes
set -g focus-events on

# Allow escape sequences to pass through to Ghostty
set -g allow-passthrough on

# Mouse support
set -g mouse on

# Status bar settings
set -g status-position top
set -g status-justify left
set -g status-left-length 100
set -g status-right-length 100

# Window auto-naming: show directory by default, prefix+, to override
set -g automatic-rename on
set -g automatic-rename-format "/#{b:pane_current_path}"

# Catppuccin base (theme.conf overrides colors; plugin provides @thm_* infrastructure)
set -g @catppuccin_flavor "mocha"

# Source theme (symlinked by theme-set, sets @thm_* color overrides)
source-file ~/.config/tmux/theme.conf

# Keep tmux split borders aligned with jankyborders active/inactive colors.
set -g @catppuccin_pane_border_style "fg=#{@thm_surface_0}"
set -g @catppuccin_pane_active_border_style "fg=#{@thm_fg}"

# Show pane number on border to indicate active pane
set -g pane-border-status top
set -g pane-border-format "#{?pane_active,#[fg=#{@thm_teal} bold] #{window_index}.#{pane_index} ,#[fg=#{@thm_overlay_0}] #{window_index}.#{pane_index} }#[default]"

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'alexwforsythe/tmux-which-key'

# Initialize TPM
run '~/.tmux/plugins/tpm/tpm'

# ── Custom status bar (Floating Islands) ─────────────────────────────────────────
# Discrete pill segments mirroring sketchybar's workspace aesthetic.
# Each piece of information floats as an independent rounded island.

# Island color: teal-tinted dark (#0d0d0d + 15% #8fbfc8)
set -g @island_active "#1e2a2d"

# Bar: deepest black background (bg0)
set -g status-style "bg=#{@thm_crust},fg=#{@thm_fg}"

# Left: session island — surface pill with bolt + session name
set -g status-left "#[fg=#{?client_prefix,#{@thm_teal},#{@thm_fg}},bg=#{@thm_crust}] ⚡ #[fg=#{@thm_fg} bold]#S#[default]   "

# Windows: active floats as teal-tinted island, inactive is bare text
set -g window-status-separator " "
set -g window-status-format "#[fg=#{?client_prefix,#{@thm_teal},#{@thm_overlay_2}},bg=#{@thm_crust}]#{?client_prefix,#[bold],} #I #[fg=#{@thm_overlay_2},nobold]#W "
set -g window-status-current-format "#[fg=#{@island_active},bg=#{@thm_crust}]#[fg=#{@thm_fg},bg=#{@island_active},bold] #I #[fg=#{@thm_fg},nobold]#W #[fg=#{@island_active},bg=#{@thm_crust}]#[default]"

# Right: info islands — git branch + running command
set -g status-right "#[fg=#{@thm_bg},bg=#{@thm_crust}]#[fg=#{@thm_green},bg=#{@thm_bg}]  #[fg=#{@thm_overlay_0}]#(cd '#{pane_current_path}' && git rev-parse --abbrev-ref HEAD 2>/dev/null) #[fg=#{@thm_bg},bg=#{@thm_crust}]#[default] #[fg=#{@thm_bg},bg=#{@thm_crust}]#[fg=#{@thm_teal},bg=#{@thm_bg}]  #[fg=#{@thm_overlay_0}]#{=30:pane_title} #[fg=#{@thm_bg},bg=#{@thm_crust}]#[default]"
