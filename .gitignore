# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Personal notes and configs
WORKING_NOTES*.md
PROJECT_NOTES*.md
sketchybar/.config/sketchybar/AGENTS.md
.gitconfig
.gitconfig-*

# OpenCode (generated by npm install)
opencode/.config/opencode/node_modules/
opencode/.config/opencode/package-lock.json

# Neovim (changes on theme switch)
nvim/.config/nvim/lazy-lock.json

# Raycast extensions (local dev only)
raycast/extensions/*/package-lock.json

# Machine-specific (not tracked)
.zshrc.local

# Aerospace (generated from profile configs)
aerospace/.config/aerospace/aerospace.toml

# Theme-managed files (theme-set creates symlinks to themes/configs/)
borders/.config/borders/bordersrc
ghostty/.config/ghostty/theme.conf

# Backups
*.backup
*.bak
backups/
.backups/
karabiner/.config/karabiner/automatic_backups/

# Clipboard cache
raycast/extensions/prompt-optimizer/.gemini-clipboard/

# Secrets (never commit)
.secrets
*.secrets
zsh/.secrets

# Gemini Clipboard
.gemini-clipboard

